#include <stdio.h>


int main() {

    const char* caminho = "arquivo_binario.bin";

    if(remove(caminho) == 0) {

        puts("Arquivo removido com sucesso!");

    } else {

        fprintf("Não foi possível remover o arquivo %s.", caminho);

        return 1;
    }

return 0;
}
